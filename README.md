deps:
* make
* python + pandoc
* nightly rust with std src
* pnpm i -D postcss postcss-cli postcss-discard-duplicates postcss-discard-overridden postcss-merge-rules cssnano

lum wasm builds from scratch, html artifacts are commited. TODO: purge automatically once git is sluggish? 